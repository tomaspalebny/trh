# Recommendations for trh

> Auto-generated by Hermes Agent  
> Created: 2026-05-06T09:24:00  
> Legend: `[ ]` pending, `[~]` in progress, `[x]` done, `[-]` dismissed

## Code Quality

<!-- Suggestions: type hints, docstrings, error handling, tests, refactoring -->

[ ] Add pytest suite covering double-auction matching edge cases: (a) bid exactly equals offer + tax — verify trade executes (boundary condition); (b) multiple unmatched bids at same price, only one matching offer — verify highest-bid-first tiebreaker doesn't starve valid matches; (c) price_floor set above all offers but below some bids — verify no trades occur despite compatible WTP/MC; (d) equilibrium calculation with unbalanced buyer/seller counts — verify `min(len(buyers), len(sellers))` correctly caps Q*.
[ ] `set_ceiling` with val <= 0 resets ceiling to 999 (unlimited). This is a hidden convention — make it explicit with a constant `CEILING_UNLIMITED = 999` and document that sending `set_ceiling` with value=0 means "remove ceiling". Currently the admin UI sends empty/0 which silently sets unlimited, which could confuse instructors.
[ ] Fix inconsistency: `try_match()` sorts bids descending and offers ascending on every call (O(n log n)). Since new bids/offers are appended one at a time, maintain sorted order on insert using `bisect.insort` or re-sort only when multiple new entries arrive in quick succession.
[ ] Add input validation for `price` in player WS: currently a non-numeric JSON value for `data["price"]` will crash with unhandled `ValueError` in `float(data["price"])`. Wrap in try/except with a Czech error response same as `bid > WTP` checks.

## Structure & Organization

<!-- Suggestions: file splitting, naming, modularity -->

[ ] Extract three HTML templates (~340 lines) from Python strings into `templates/landing.html`, `templates/player.html`, `templates/admin.html`. trh has the best overall documentation but the same inline-HTML-in-Python anti-pattern as all sibling repos.
[ ] Move market matching engine into `matching.py`: extract `try_match()`, equilibrium calculation `_calc_equilibrium()`, and price validation logic into a dedicated `DoubleAuctionMatcher` class. This is the most complex matching algorithm across all 5 repos (real-time double auction with tax/floor/ceiling constraints) and deserves clean isolation.
[ ] Make WTP and MC ranges configurable via env vars instead of hardcoded: `WTP_MIN=30`, `WTP_MAX=100`, `MC_MIN=10`, `MC_MAX=70`. Currently changing these requires editing source code. Different seminar scenarios (high-value market vs low-value market) should be deployable without code changes.
[ ] Add env-based configuration for all default parameters: `DEFAULT_MAX_ROUNDS`, `DEFAULT_TAX`, `DEFAULT_PRICE_FLOOR`, `DEFAULT_PRICE_CEILING`. This enables pre-configured "shock scenario" deployments — e.g., a tax-heavy deployment for tax incidence seminars.

## Documentation

<!-- Suggestions: README improvements, inline docs, examples -->

[ ] trh/doc.md is already excellent — the best documentation across all 5 repos. Add market shock diversity: currently only tax, price floor, and price ceiling are implemented. Document roadmap items for subsidy (negative tax), quota (maximum trades per round), and supply/demand curve shift shocks (e.g., "harvest failure" raises all MC by 20) to enrich the pedagogical toolkit beyond current 3 levers.
[ ] Add inline comments in `try_match()` explaining the double-auction algorithm's efficiency properties — specifically that sorting bids descending and offers ascending and greedily matching is equivalent to the maximum-volume matching under uniform-price double auction rules. This connects code to experimental economics literature (Vernon Smith, 1962).
[ ] Document the alternating role assignment (`len(players) % 2 == 0 → buyer`) explicitly in the landing page or lobby screen so late-joining students understand their role isn't random but deterministic. Currently this can cause confusion when two friends both expect to be buyers.
[ ] Add i18n infrastructure planning to doc.md: currently all UI text is hardcoded Czech. While the primary audience is Czech economics students, adding a simple `lang=` query parameter with a translations dict would enable reuse by international programs without duplicating the entire codebase.

## Performance & Security

<!-- Suggestions: bottlenecks, dependencies, secrets handling -->

[ ] The `broadcast_state()` function sends full game state (including leaderboard, round_history, recent_trades) on every broadcast. For active trading phases with 20+ players and rapid bid/offer updates, this generates significant WebSocket traffic. Consider sending delta updates: only changed fields, not full state snapshots on every bid submission.
[ ] Silent exception swallowing in `broadcast_room()` (`except: pass`) hides connection failures. Replace with `logger.warning(f"Failed to send to {room_id}: {e}")` for debuggability.
[ ] No protection against room enumeration: a curious student can increment room IDs in `/admin/{room_id}` and access any active seminar's admin panel. Add optional `ADMIN_PASSWORD` env var with a simple token check — even a shared seminar password would deter casual interference.
[ ] In `try_match()`, after a trade is matched both bid and offer are marked `matched=True` but never removed from the lists. Over many rounds, accumulated dead entries bloat `self.bids` and `self.offers` arrays causing O(n²) scanning overhead. Clear them at the start of each `start_round()` as currently done — but also consider immediate removal from list on match to keep live-phase iteration efficient.

---

*Last updated: 2026-05-06T09:24:00*